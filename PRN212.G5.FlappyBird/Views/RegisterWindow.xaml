<Window x:Class="PRN212.G5.FlappyBird.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng ký - Flappy Bird" 
        Height="700" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F4F8">
    <Grid>
        <!-- Background gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#f093fb" Offset="0"/>
                <GradientStop Color="#f5576c" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Border CornerRadius="25" 
                    Background="White"
                    Margin="30"
                    Padding="45"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="8" Color="#40000000" Opacity="0.4"/>
                </Border.Effect>

                <StackPanel>
                    <!-- Logo/Title -->
                    <Viewbox Width="80" Height="80" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <TextBlock Text="🐦" FontSize="60"/>
                    </Viewbox>
                    
                    <TextBlock Text="Tạo tài khoản mới" 
                               FontSize="32" 
                               FontWeight="Bold"
                               Foreground="#2C3E50"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,5">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Color="#CCCCCC" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <TextBlock Text="Đăng ký để lưu điểm cao của bạn" 
                               FontSize="14"
                               Foreground="#7F8C8D"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,35"/>

                    <!-- Name field -->
                    <TextBlock Text="👤 Tên hiển thị" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#34495E"
                               Margin="0,0,0,8"/>
                    
                    <TextBox x:Name="NameTextBox"
                             FontSize="15"
                             Padding="15,12"
                             BorderBrush="#D5DBDB"
                             BorderThickness="2"
                             Background="#F8F9FA"
                             Margin="0,0,0,20"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border x:Name="border" 
                                                    CornerRadius="8"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Background="{TemplateBinding Background}">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#f5576c"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Email field -->
                    <TextBlock Text="📧 Email" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#34495E"
                               Margin="0,0,0,8"/>
                    
                    <TextBox x:Name="EmailTextBox"
                             FontSize="15"
                             Padding="15,12"
                             BorderBrush="#D5DBDB"
                             BorderThickness="2"
                             Background="#F8F9FA"
                             Margin="0,0,0,20"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border x:Name="border" 
                                                    CornerRadius="8"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Background="{TemplateBinding Background}">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#f5576c"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Password field -->
                    <TextBlock Text="🔒 Mật khẩu" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#34495E"
                               Margin="0,0,0,8"/>
                    
                    <PasswordBox x:Name="PasswordBox"
                                 FontSize="15"
                                 Padding="15,12"
                                 BorderBrush="#D5DBDB"
                                 BorderThickness="2"
                                 Background="#F8F9FA"
                                 Margin="0,0,0,20"
                                 GotFocus="PasswordBox_GotFocus"
                                 LostFocus="PasswordBox_LostFocus"
                                 KeyDown="PasswordBox_KeyDown">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="PasswordBox">
                                            <Border x:Name="border" 
                                                    CornerRadius="8"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Background="{TemplateBinding Background}">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#f5576c"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>

                    <!-- Confirm Password field -->
                    <TextBlock Text="🔒 Xác nhận mật khẩu" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#34495E"
                               Margin="0,0,0,8"/>
                    
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 FontSize="15"
                                 Padding="15,12"
                                 BorderBrush="#D5DBDB"
                                 BorderThickness="2"
                                 Background="#F8F9FA"
                                 Margin="0,0,0,20"
                                 GotFocus="PasswordBox_GotFocus"
                                 LostFocus="PasswordBox_LostFocus"
                                 KeyDown="PasswordBox_KeyDown">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="PasswordBox">
                                            <Border x:Name="border" 
                                                    CornerRadius="8"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Background="{TemplateBinding Background}">
                                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#f5576c"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>

                    <!-- Avatar section -->
                    <TextBlock Text="🖼️ Ảnh đại diện (tùy chọn)" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="#34495E"
                               Margin="0,0,0,10"/>
                    
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Avatar preview -->
                        <Border Grid.Column="0"
                                Width="90" Height="90"
                                CornerRadius="45"
                                Background="#E0E0E0"
                                BorderBrush="#f5576c"
                                BorderThickness="3"
                                Margin="0,0,15,0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="#40000000" Opacity="0.2"/>
                            </Border.Effect>
                            <Grid>
                                <Image x:Name="AvatarPreview"
                                       Stretch="UniformToFill"
                                       ClipToBounds="True">
                                    <Image.Clip>
                                        <EllipseGeometry Center="45,45" RadiusX="45" RadiusY="45"/>
                                    </Image.Clip>
                                </Image>
                                <TextBlock Text="👤" 
                                           FontSize="40"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           x:Name="DefaultAvatarText"/>
                            </Grid>
                        </Border>

                        <!-- Avatar buttons -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button x:Name="SelectAvatarButton"
                                    Content="📁 Chọn ảnh"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Padding="18,10"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="SelectAvatarButton_Click"
                                    Margin="0,0,0,8">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#f093fb" Offset="0"/>
                                        <GradientStop Color="#f5576c" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <Button.Foreground>
                                    <SolidColorBrush Color="White"/>
                                </Button.Foreground>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border"
                                                            CornerRadius="8"
                                                            Background="{TemplateBinding Background}"
                                                            Padding="{TemplateBinding Padding}">
                                                        <Border.Effect>
                                                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Color="#40000000" Opacity="0.2"/>
                                                        </Border.Effect>
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button x:Name="RemoveAvatarButton"
                                    Content="Xóa ảnh"
                                    FontSize="13"
                                    Padding="15,8"
                                    Background="#E74C3C"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="RemoveAvatarButton_Click"
                                    Visibility="Collapsed">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="border"
                                                            CornerRadius="6"
                                                            Background="{TemplateBinding Background}"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#C0392B"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Error message -->
                    <TextBlock x:Name="ErrorMessageText"
                               Text=""
                               Foreground="#E74C3C"
                               FontSize="12"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"
                               Visibility="Collapsed"
                               TextWrapping="Wrap"/>

                    <!-- Register button -->
                    <Button x:Name="RegisterButton"
                            FontSize="17"
                            FontWeight="Bold"
                            Padding="0,15"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="RegisterButton_Click"
                            Margin="0,5,0,20">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="✨" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="Đăng ký" FontSize="17"/>
                            </StackPanel>
                        </Button.Content>
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#f093fb" Offset="0"/>
                                <GradientStop Color="#f5576c" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Foreground>
                            <SolidColorBrush Color="White"/>
                        </Button.Foreground>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border"
                                                    CornerRadius="10"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}">
                                                <Border.Effect>
                                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Color="#40000000" Opacity="0.3"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                                    <Setter TargetName="border" Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Color="#40000000" Opacity="0.4"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Login link -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Đã có tài khoản? " 
                                   FontSize="14"
                                   Foreground="#7F8C8D"/>
                        <TextBlock Text="Đăng nhập" 
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="#f5576c"
                                   Cursor="Hand"
                                   MouseDown="LoginLink_MouseDown">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#f093fb"/>
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Window>

